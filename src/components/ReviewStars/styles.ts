import { styled } from "@/lib/stitches.config";

export const StarsButtonsContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  gap: '$1'
})

export const StarButtonEmpty = styled("button", {});
